{
  "name": "ts-test-action",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx ncc build src/index.ts",
    "build:watch": "npm run build -- --watch"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "@vercel/ncc": "^0.38.1",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  }
}
